<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Fundo branco (opcional) -->
    <corners android:radius="50dp" /> <!-- Faz com que o fundo seja circular -->
</shape>
